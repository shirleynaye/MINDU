<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootBlog"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/aliceblue"
    tools:context="ec.repositoriocompartido.MINDUApp.BlogActivity">

    <!-- Imagen superior izquierda -->
    <ImageView
        android:id="@+id/imgTopLeft"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:contentDescription="logo izquierda"
        android:src="@drawable/volver"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Imagen superior derecha -->
    <ImageView
        android:id="@+id/imgTopRight"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:contentDescription="logo derecha"
        android:src="@drawable/avatar2"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Título centrado debajo de las imágenes -->
    <TextView
        android:id="@+id/tvMiCuentax"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Blog Educativo"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/imgTopLeft"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
    <TextView
        android:id="@+id/tvMiCuentaxx"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Midfulness y Bienestar Psicologico"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvMiCuentax"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Scroll con 4 imágenes grandes -->
    <androidx.core.widget.NestedScrollView
        android:id="@+id/scrollImages"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/tvMiCuentaxx"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:paddingBottom="16dp">

            <!-- Imagen 1 -->
            <ImageView
                android:id="@+id/imgLarge1"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adjustViewBounds="true"
                android:scaleType="fitCenter"
                android:layout_marginBottom="12dp"
                android:contentDescription="Imagen 1"
                android:src="@drawable/pdf1" />

            <!-- Imagen 2 -->
            <ImageView
                android:id="@+id/imgLarge2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adjustViewBounds="true"
                android:scaleType="fitCenter"
                android:layout_marginBottom="12dp"
                android:contentDescription="Imagen 2"
                android:src="@drawable/pdf2" />

            <!-- Imagen 3 -->
            <ImageView
                android:id="@+id/imgLarge3"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adjustViewBounds="true"
                android:scaleType="fitCenter"
                android:layout_marginBottom="12dp"
                android:contentDescription="Imagen 3"
                android:src="@drawable/pdf3" />

            <!-- Imagen 4 -->
            <ImageView
                android:id="@+id/imgLarge4"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adjustViewBounds="true"
                android:scaleType="fitCenter"
                android:layout_marginBottom="12dp"
                android:contentDescription="Imagen 4"
                android:src="@drawable/pdf4" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
