<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo -->
    <solid android:color="@color/white" />

    <!-- Bordes redondeados -->
    <corners android:radius="16dp" />

    <!-- Opcional: borde (stroke) -->
    <stroke
        android:width="2dp"
        android:color="@color/violet" />
</shape>
